@{
    ViewData["Title"] = "Truy cập bị từ chối";
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="~/css/access-denied.css" />
</head>
<body>
    <div class="main-content">
        <div class="error-card">
            <!-- Icon -->
            <div class="mb-4">
                <i class="fas fa-lock" style="font-size: 64px; color: #dc3545;"></i>
            </div>

            <!-- Title -->
            <h2 class="mb-3" style="color: #333; font-weight: 600;">Truy cập bị từ chối</h2>

            <!-- Description -->
            <p class="text-muted mb-4">
                Bạn không có quyền truy cập vào trang này. Vui lòng kiểm tra lại
                quyền hạn của tài khoản hoặc liên hệ với quản trị viên.
            </p>

            <!-- Action Buttons -->
            <div class="d-flex gap-2 justify-content-center flex-wrap">
                <a href="@Url.Action("Index", "Client")" class="btn btn-primary px-4">
                    <i class="fas fa-home me-2"></i>Về trang chủ
                </a>
                <a href="@Url.Action("Logout", "Account")" class="btn btn-outline-secondary px-4">
                    <i class="fas fa-sign-out-alt me-2"></i>Đăng xuất
                </a>
            </div>
        </div>
    </div>
</body>
</html>
